import { PassuLogo } from "@passu/ui/passu-logo";

export default function SideBar() {
  return (
    <aside className="flex justify-center px-12 pt-28">
      <PassuLogo className={`w-full`} />
    </aside>
  );
}
